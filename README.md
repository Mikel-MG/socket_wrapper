# socket_wrapper

Python wrapper for the Woolfon Group’s SOCKET KIH detection tool

## Installation

```bash
# download, package, and install socket_wrapper
pip install git+https://github.com/Mikel-MG/socket_wrapper.git

# post-installation check for binaries and DSSP's component library
# (will be downloaded if necessary)
socket_check_install
```
